# Production values must be stored as Sites secrets. Never place the password or these values in client code.
# Generate via createPasswordVerifier() and 32 cryptographically random bytes (base64url).
ROOM_PASSWORD_VERIFIER=
ROOM_SESSION_SECRET=
