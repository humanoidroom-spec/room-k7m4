import { useState } from 'react';
import { asset, models, representations } from '../data/project';
import { Arrow, SectionLabel } from '../components/Primitives';
export default function Method() {
  const [selected,setSelected] = useState(1);
  const method = representations[selected];
  return <section id="method" className="section method wrap"><SectionLabel number="05">Nonverbal → action</SectionLabel><h2>From human behavior<br />to <em>robot action.</em></h2><div className="behavior-story">{[['Human points.','“That object.”','WHAT'],['Human begins to sit.','“Now.”','WHEN'],['Human corrects.','“Change the plan.”','HOW']].map(([cue,intent,word]) => <div key={word}><span>{word}</span><p>{cue}</p><Arrow /><strong>{intent}</strong></div>)}</div><div className="method-intro"><p>How should a policy receive the signal?</p><span>CONTROLLED COMPARISON · {models[2]}</span></div><div className="segmented method-tabs" aria-label="Nonverbal representations">{representations.map((item,i) => <button type="button" key={item.id} aria-pressed={selected===i} className={selected===i?'selected':''} onClick={() => setSelected(i)}>{item.name}</button>)}</div><div className="architecture-flow" aria-label={`${method.name} integration diagram`}><div className="flow-block"><span>01 / OBSERVE</span><h3>{method.input}</h3><p>Raw human behavior</p></div><Arrow /><div className="flow-block emphasis"><span>02 / REPRESENT</span><h3>{method.encoding}</h3><p>{method.label}</p></div><Arrow /><div className="flow-block"><span>03 / GROUND</span><h3>{method.integration}</h3><p>{models[2]}</p></div><Arrow /><div className="flow-block action"><span>04 / RESPOND</span><h3>Robot action</h3><p>Action expert</p></div></div><div className="method-explanation" aria-live="polite"><div><h3>{method.conclusion}</h3><p>{method.description}</p></div><div className="method-finding"><span className="control-label">WHAT THE EXPERIMENTS SHOW</span><p>{method.finding}</p><span className="source-note">{method.source}</span></div></div><details className="figure-detail"><summary>Inspect the full architecture from the paper <span>FIG. 03 <span>+</span></span></summary><figure><img src={asset('assets/architecture.webp')} alt="Original ROOM architecture figure showing nonverbal representations, visual and semantic prompting, behavior context and behavior module integration" width="3094" height="1357" loading="lazy" /><figcaption>Fig. 3 from the supplied draft. Its row C repeats the spatiotemporal label; the semantic pathway above follows the terminology in §IV-B.</figcaption></figure></details></section>;
}
