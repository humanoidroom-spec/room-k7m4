import { project, asset } from '../data/project';
import { Arrow, ResearchMedia } from '../components/Primitives';
export default function Hero() {
  return <section className="hero" id="overview"><div className="hero-intro wrap"><div className="eyebrow">HUMAN–HUMANOID INTERACTION <span>DATASET & BENCHMARK</span></div><div className="hero-heading"><h1>Can humanoids<br />read the <em>room?</em></h1><div className="hero-side"><p>Human gaze. Human gesture.<br />Human posture. <span>Robot action.</span></p><div className="hero-description">Grounding nonverbal human behavior<br className="desktop-break" /> in humanoid manipulation.</div><a href="#tasks" className="text-link">Explore the interactions <Arrow /></a></div></div><div className="hero-actions"><a className="button primary" href={(project.links.arxiv ?? asset(project.links.paper))} target="_blank" rel="noreferrer">Read the paper <Arrow diagonal /></a><a className="button quiet" href={project.links.dataset ?? "#resources"}>Dataset {!project.links.dataset && <span className="soon">Coming soon</span>}</a><a className="button quiet" href={project.links.code ?? "#resources"}>Code {!project.links.code && <span className="soon">Coming soon</span>}</a></div></div><div className="hero-visual"><ResearchMedia name={project.media.hero} poster={project.media.heroPoster} alt="Real ROOM experiments: a human and Unitree G1 collaborate on object handover, cart manipulation and tabletop interaction in a home environment." eager /><div className="hero-visual-top"><span>ROOM / FIELD OBSERVATIONS</span><span>UNITREE G1 · HUMAN IN THE LOOP</span></div><div className="hero-visual-bottom"><span className="scene-caption">A shared space.<br /><strong>A shared understanding.</strong></span><span className="source-stamp">EXPERIMENT SCENES · FIG. 01</span></div></div><div className="hero-bottom wrap"><span>Beyond executing tasks.<br /><strong>Understanding the person behind them.</strong></span><a href="#why" aria-label="Scroll to why ROOM"><span>SCROLL TO DISCOVER</span><span className="down-arrow">↓</span></a></div></section>;
}
